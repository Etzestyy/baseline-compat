    - name: Post PR Comment
      if: github.event_name == 'pull_request'
      uses: actions/github-script@v7
      with:
        script: |
          const fs = require('fs');
          const comment = fs.readFileSync('baseline-pr-comment.md', 'utf-8');
          github.rest.issues.createComment({
            issue_number: context.issue.number,
            owner: context.repo.owner,
            repo: context.repo.repo,
            body: comment
          });
name: 'Baseline Compat Scan'
description: 'Scan repo for risky web features and enforce Baseline threshold.'
inputs:
  threshold:
    description: 'Minimum compatibility percentage required.'
    required: false
    default: '95'
runs:
  using: 'composite'
  steps:
    - name: Checkout
      uses: actions/checkout@v4
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
    - name: Install dependencies
      run: npm ci
    - name: Run Baseline Scan
      run: |
        node cli/scan.js .
      env:
        BASELINE_THRESHOLD: ${{ inputs.threshold }}
    - name: Upload SARIF
      uses: github/code-scanning-action/upload-sarif@v2
      with:
        sarif_file: baseline-report.sarif
